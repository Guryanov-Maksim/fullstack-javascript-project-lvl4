extends ../layouts/application.pug

block header
  = t('views.labels.edit.header')

block content
  .card
    .card-body.p-5.bg-
      +formFor()(action=route('updateLabels', label.id) method='post')
        +input(label, 'name', errors, 'labels')(type="text")
        +buttonTo(route('updateLabels', label.id), 'patch')(class="btn btn-primary" value=t('views.labels.edit.change'))
